# UML Class Diagram Standards

- Visibility indicators: `+` (public), `-` (private), `#` (protected), `~` (package).
- Relationships:
  - Association (`-->` or `--`)
  - Aggregation (`o--`)
  - Composition (`*--`)
  - Generalization/Inheritance (`--|>`)
  - Realization/Implementation (`..|>`)
